global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yaml

scrape_configs:
  - job_name: mdb-mcp-gateway
    metrics_path: /metrics
    static_configs:
      - targets:
          - gateway:8000
